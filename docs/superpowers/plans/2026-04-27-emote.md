# Emote 系統實作計畫（B3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在三款戰鬥（BR / Items / Territory）中加入 hold T + 1-6 觸發顏文字 emote，自身角色頭頂顯示 2.5s float-up bubble，三款共用。

**Architecture:** Server 為權威；client INPUT 多帶 `emote: 1-6 | null`，server 共用 `applyEmoteInput()` helper（cooldown / 範圍 / paused 過濾），合法則 push `{kind:'emote'}` 進 `state.events`，靠既有 SNAPSHOT events slice 機制廣播。Client 用 `useEmoteInput()` 監聽 hold T，`useEmoteFeed()` 訂閱 SNAPSHOT 累積活著的 emote，三款 Arena 各加一層 `<EmoteBubble>` SVG 渲染。完全不開新 socket event、不動 DB schema。

**Tech Stack:** ES modules、`node:test` + `node:assert/strict`、React 18、Vite、socket.io、SVG。

**Spec:** `docs/superpowers/specs/2026-04-27-emote-design.md`

---

## File Structure

### 新增檔案

| 檔案 | 職責 |
|------|------|
| `packages/shared/src/emotes.js` | `EMOTES` 清單、`EMOTE_CD_MS` 常數、`applyEmoteInput()` helper |
| `packages/shared/test/emotes.test.js` | applyEmoteInput 單元測試 |
| `packages/client/src/components/EmoteBar.jsx` | hold T 期間畫面下方 6 格 bar |
| `packages/client/src/components/EmoteBubble.jsx` | SVG 頭頂氣泡 |
| `packages/client/src/screens/battle/useEmoteInput.js` | hold T + 1-6 偵測 hook |
| `packages/client/src/screens/battle/useEmoteFeed.js` | 訂閱 SNAPSHOT 累積活著的 emote |

### 修改檔案

| 檔案 | 變更摘要 |
|------|---------|
| `packages/shared/src/index.js` | re-export `./emotes.js` |
| `packages/shared/src/games/br/simulation.js` | createInitialState + sanitizeInput + applyInput |
| `packages/shared/src/games/items/simulation.js` | createInitialState + sanitizeInput + applyInput |
| `packages/shared/src/games/territory/simulation.js` | createInitialState + sanitizeInput + applyInput |
| `packages/shared/test/games/inputSchema.test.js` | 加 emote 三款白名單測試 |
| `packages/client/index.html` | 加 `emoteBubbleFloat` keyframe |
| `packages/client/src/screens/battle/br/useInputBR.js` | 接 `emoteOpen` 第三 arg（passthrough，不影響邏輯） |
| `packages/client/src/screens/battle/items/useInputItems.js` | 接 `emoteOpen`，true 時 1-5 不寫 pendingSkill |
| `packages/client/src/screens/battle/territory/useInputTerritory.js` | 接 `emoteOpen` arg（passthrough） |
| `packages/client/src/screens/battle/br/BattleRoyale.jsx` | 整合 useEmoteInput / useEmoteFeed / EmoteBar；INPUT 帶 emote |
| `packages/client/src/screens/battle/br/ArenaBR.jsx` | 接 activeEmotes prop、新增 EmoteBubble 層 |
| `packages/client/src/screens/battle/items/ItemsBattle.jsx` | 同 BR |
| `packages/client/src/screens/battle/items/ArenaItems.jsx` | 同 BR |
| `packages/client/src/screens/battle/territory/TerritoryBattle.jsx` | 同 BR |
| `packages/client/src/screens/battle/territory/ArenaTerritory.jsx` | 同 BR |
| `packages/server/test/smoke.js` | 加 emote 場景驗證 |

### 不動的部分

- `packages/shared/src/protocol.js` — `MSG` 物件不加 event
- `packages/server/src/socketHandlers.js` — INPUT pipeline 通用
- `packages/server/src/match.js` — events slice 機制通用
- `packages/server/src/services/chatService.js` — 與 chat 完全隔離
- `packages/server/prisma/schema.prisma` — 不持久化
- `packages/client/src/screens/SpectatorBattle.jsx` — readOnly 路徑透過共用 BattleXxx 元件自動 work

---

# Phase A：Shared 層（server + client 共用）

## Task 1: shared/emotes.js — EMOTES + EMOTE_CD_MS + applyEmoteInput

**Files:**
- Create: `packages/shared/test/emotes.test.js`
- Create: `packages/shared/src/emotes.js`

- [ ] **Step 1: 寫失敗測試**

建立 `packages/shared/test/emotes.test.js`：

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { EMOTES, EMOTE_CD_MS, applyEmoteInput } from '../src/emotes.js';

test('emotes: 共 6 個 slot 且 slot 1-6 連續', () => {
  assert.equal(EMOTES.length, 6);
  EMOTES.forEach((e, i) => {
    assert.equal(e.slot, i + 1);
    assert.equal(typeof e.kaomoji, 'string');
    assert.ok(e.kaomoji.length > 0);
    assert.equal(typeof e.label, 'string');
  });
});

test('emotes: EMOTE_CD_MS = 3000', () => {
  assert.equal(EMOTE_CD_MS, 3000);
});

function makePlayer(over = {}) {
  return { id: 'p1', paused: false, alive: true, emoteCdUntil: 0, ...over };
}

function makeState() {
  return { events: [] };
}

test('applyEmoteInput: 合法 slot 1-6 + 無 cooldown → push event + 設 cooldown', () => {
  const p = makePlayer();
  const s = makeState();
  applyEmoteInput(p, { emote: 3 }, s, 1000);
  assert.equal(s.events.length, 1);
  assert.equal(s.events[0].kind, 'emote');
  assert.equal(s.events[0].playerId, 'p1');
  assert.equal(s.events[0].slot, 3);
  assert.equal(s.events[0].atMs, 1000);
  assert.equal(p.emoteCdUntil, 1000 + EMOTE_CD_MS);
});

test('applyEmoteInput: cooldown 期間再按 → noop', () => {
  const p = makePlayer({ emoteCdUntil: 5000 });
  const s = makeState();
  applyEmoteInput(p, { emote: 1 }, s, 4999);
  assert.equal(s.events.length, 0);
  assert.equal(p.emoteCdUntil, 5000);
});

test('applyEmoteInput: cooldown 邊界 (now === emoteCdUntil) → 允許', () => {
  const p = makePlayer({ emoteCdUntil: 5000 });
  const s = makeState();
  applyEmoteInput(p, { emote: 1 }, s, 5000);
  assert.equal(s.events.length, 1);
});

test('applyEmoteInput: slot 越界 → noop', () => {
  for (const bad of [0, 7, -1, 100]) {
    const p = makePlayer();
    const s = makeState();
    applyEmoteInput(p, { emote: bad }, s, 1000);
    assert.equal(s.events.length, 0, `slot=${bad} 應被擋`);
    assert.equal(p.emoteCdUntil, 0);
  }
});

test('applyEmoteInput: 非整數 slot → noop', () => {
  for (const bad of [1.5, 'foo', {}, true]) {
    const p = makePlayer();
    const s = makeState();
    applyEmoteInput(p, { emote: bad }, s, 1000);
    assert.equal(s.events.length, 0);
  }
});

test('applyEmoteInput: emote = null → noop（一般 tick）', () => {
  const p = makePlayer();
  const s = makeState();
  applyEmoteInput(p, { emote: null }, s, 1000);
  assert.equal(s.events.length, 0);
  assert.equal(p.emoteCdUntil, 0);
});

test('applyEmoteInput: 沒帶 emote 欄位 → noop', () => {
  const p = makePlayer();
  const s = makeState();
  applyEmoteInput(p, {}, s, 1000);
  assert.equal(s.events.length, 0);
});

test('applyEmoteInput: player.paused → noop', () => {
  const p = makePlayer({ paused: true });
  const s = makeState();
  applyEmoteInput(p, { emote: 6 }, s, 1000);
  assert.equal(s.events.length, 0);
});

test('applyEmoteInput: player.alive === false → 仍會發（刻意允許）', () => {
  const p = makePlayer({ alive: false });
  const s = makeState();
  applyEmoteInput(p, { emote: 3 }, s, 1000);
  assert.equal(s.events.length, 1);
  assert.equal(s.events[0].slot, 3);
});

test('applyEmoteInput: 玩家初次 emote（emoteCdUntil 未初始化也行）', () => {
  const p = { id: 'p1', paused: false, alive: true };  // 沒有 emoteCdUntil
  const s = makeState();
  applyEmoteInput(p, { emote: 1 }, s, 1000);
  assert.equal(s.events.length, 1);
  assert.equal(p.emoteCdUntil, 1000 + EMOTE_CD_MS);
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
node --test packages/shared/test/emotes.test.js
```

預期：`Cannot find module '../src/emotes.js'` 或全部 fail。

- [ ] **Step 3: 寫最小實作**

建立 `packages/shared/src/emotes.js`：

```js
// 戰鬥內快捷表情（顏文字）— 三款遊戲共用。
// 觸發機制：client INPUT 帶 emote: 1-6 | null；server 過 applyEmoteInput
// 檢查 cooldown / 範圍 / paused，合法則寫入 state.events。

export const EMOTE_CD_MS = 3000;

export const EMOTES = [
  { slot: 1, key: '1', kaomoji: '(´∀｀)',         label: '笑' },
  { slot: 2, key: '2', kaomoji: '＼(^o^)／',      label: '拍手' },
  { slot: 3, key: '3', kaomoji: '(╥﹏╥)',         label: '哭' },
  { slot: 4, key: '4', kaomoji: '(°ロ°)',         label: '驚' },
  { slot: 5, key: '5', kaomoji: 'ಠ_ಠ',           label: '嘲諷' },
  { slot: 6, key: '6', kaomoji: '(╯°□°)╯︵ ┻━┻', label: '翻桌' },
];

/**
 * 三款 sim 的 applyInput 末段都呼叫此 helper。
 * 死亡 / 凍結 / silenced 不擋（emote 是社交訊號，不是遊戲動作）。
 * 只擋：non-integer slot、越界、玩家 paused（老闆鍵）、cooldown 中。
 *
 * @param {object} player — state.players[id]，會就地修改 emoteCdUntil
 * @param {object} input — sanitize 後的 INPUT（input.emote 為 1-6 | null）
 * @param {object} state — { events: [...] }，會就地 push 事件
 * @param {number} now — 絕對 ms 時間戳
 */
export function applyEmoteInput(player, input, state, now) {
  if (input == null || input.emote == null) return;
  if (!Number.isInteger(input.emote)) return;
  if (input.emote < 1 || input.emote > EMOTES.length) return;
  if (player.paused) return;
  if (now < (player.emoteCdUntil || 0)) return;
  player.emoteCdUntil = now + EMOTE_CD_MS;
  state.events.push({
    kind: 'emote',
    playerId: player.id,
    slot: input.emote,
    atMs: now,
  });
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
node --test packages/shared/test/emotes.test.js
```

預期：所有測試 PASS。

- [ ] **Step 5: 跑 shared workspace 全測試確認沒打到既有檔**

```bash
npm test --workspace @office-colosseum/shared
```

預期：全部 PASS（既有 + 新加 emotes.test.js）。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/emotes.js packages/shared/test/emotes.test.js
git commit -m "feat(shared): 加 emotes.js — EMOTES 清單與 applyEmoteInput helper"
```

---

## Task 2: shared/index.js — re-export emotes

**Files:**
- Modify: `packages/shared/src/index.js`

- [ ] **Step 1: 改 index.js 加一行 re-export**

`packages/shared/src/index.js` 改成：

```js
export * from './constants.js';
export * from './math.js';
export * from './characters.js';
export * from './protocol.js';
export * from './chatSanitize.js';
export * from './emotes.js';
```

- [ ] **Step 2: 確認 re-export 可用**

```bash
node -e "import('./packages/shared/src/index.js').then(m => console.log(Object.keys(m).filter(k => k.startsWith('EMOTE') || k === 'applyEmoteInput')))"
```

預期 stdout 包含：`['EMOTE_CD_MS', 'EMOTES', 'applyEmoteInput']`。

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/index.js
git commit -m "feat(shared): index.js re-export emotes"
```

---

## Task 3: BR sim 接入 emote

**Files:**
- Modify: `packages/shared/src/games/br/simulation.js`

- [ ] **Step 1: 為 BR createInitialState 加 emoteCdUntil 欄位**

找 `createInitialState` 內 `state.players[p.id] = {...}` 區塊（約 line 126-146），在 `lastHurtAt: 0,` 後面加：

```js
      lastPoisonTickAt: 0,
      lastHurtAt: 0,
      emoteCdUntil: 0,
    };
```

- [ ] **Step 2: 為 BR sanitizeInput 加 emote 白名單**

找 `sanitizeInput`（line 156-168）改成：

```js
export function sanitizeInput(raw) {
  if (!raw || typeof raw !== 'object') return null;
  const num = (v) => (Number.isFinite(v) ? v : 0);
  const emote = (Number.isInteger(raw.emote) && raw.emote >= 1 && raw.emote <= 6)
    ? raw.emote
    : null;
  return {
    seq: Number.isFinite(raw.seq) ? (raw.seq | 0) : 0,
    moveX: num(raw.moveX),
    moveY: num(raw.moveY),
    aimAngle: num(raw.aimAngle),
    attack: !!raw.attack,
    shield: !!raw.shield,
    dash: !!raw.dash,
    emote,
  };
}
```

- [ ] **Step 3: 為 BR applyInput 末段呼叫 helper**

檔案頂端 import 區補一行：

```js
import { applyEmoteInput } from '../../emotes.js';
```

`applyInput` 函式的 `return state;`（line 242）前加：

```js
  applyEmoteInput(p, input, state, now);

  return state;
}
```

注意：因為 BR 的 `applyInput` 開頭有 `if (!p || !p.alive || p.paused) return state;`（line 174），死亡或 paused 的玩家根本不會走到這裡。**這跟 spec「死了仍能 emote」衝突**——必須改條件，讓死亡/paused 玩家還是能跑到 emote 邏輯。

把 `applyInput` 改成：

```js
export function applyInput(state, playerId, input, now, rng = Math.random) {
  const p = state.players[playerId];
  if (!p) return state;

  // emote 邏輯不受 alive / paused 影響（除了 paused 在 helper 內擋）
  // 但 paused / 死亡狀態下其他輸入仍應 short-circuit
  if (!p.alive || p.paused) {
    applyEmoteInput(p, input, state, now);
    return state;
  }

  // 移動向量 ... [既有邏輯不動] ...
  // ... 既有的 aim / shield / dash / attack 全保留 ...

  applyEmoteInput(p, input, state, now);

  return state;
}
```

實際上：
- 把開頭的 `if (!p || !p.alive || p.paused) return state;` 改為兩段 early return
- 末段加 `applyEmoteInput(p, input, state, now);`

- [ ] **Step 4: 寫一個整合測試確認 BR sim 會發出 emote 事件**

`packages/shared/test/games/br/simulation.test.js` 開頭 import 加 `applyEmoteInput`、檔尾加：

```js
test('br/applyInput：合法 emote → state.events 有 emote 事件', () => {
  const s = createInitialState(players2, config0, startedAtMs);
  const before = s.events.length;
  applyInput(s, 'p1', emptyInput({ emote: 6 }), 1000);
  const emoteEvents = s.events.slice(before).filter(e => e.kind === 'emote');
  assert.equal(emoteEvents.length, 1);
  assert.equal(emoteEvents[0].slot, 6);
  assert.equal(emoteEvents[0].playerId, 'p1');
});

test('br/applyInput：玩家死亡仍能 emote（社交訊號）', () => {
  const s = createInitialState(players2, config0, startedAtMs);
  s.players.p1.alive = false;
  applyInput(s, 'p1', emptyInput({ emote: 1 }), 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 1);
});

test('br/applyInput：paused 玩家不能 emote', () => {
  const s = createInitialState(players2, config0, startedAtMs);
  s.players.p1.paused = true;
  applyInput(s, 'p1', emptyInput({ emote: 1 }), 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 0);
});
```

注意：`emptyInput()` 已經 spread `{...over}`，傳 `{ emote: 6 }` 會被 attach 上去。

- [ ] **Step 5: 跑 BR 測試確認通過**

```bash
node --test packages/shared/test/games/br/simulation.test.js
```

預期全 PASS。若 fail，多半是 `applyInput` 的 early return 漏改。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/games/br/simulation.js packages/shared/test/games/br/simulation.test.js
git commit -m "feat(br): applyInput 接 emote helper、createInitialState 加 emoteCdUntil"
```

---

## Task 4: Items sim 接入 emote

**Files:**
- Modify: `packages/shared/src/games/items/simulation.js`

- [ ] **Step 1: 為 Items createInitialState 加 emoteCdUntil 欄位**

找 `createInitialState` 的 `state.players[p.id] = {...}`（line 64-82），在 `lastHpRecordAt: startedAtMs,` 後加：

```js
      hpHistory: [{ atMs: startedAtMs, hp: MAX_HP }],
      lastHurtAt: 0, lastHpRecordAt: startedAtMs,
      emoteCdUntil: 0,
    };
```

- [ ] **Step 2: 為 Items sanitizeInput 加 emote 白名單**

找 `sanitizeInput`（line 98-112）改成：

```js
export function sanitizeInput(raw) {
  if (!raw || typeof raw !== 'object') return null;
  const num = (v) => (Number.isFinite(v) ? v : 0);
  const skill = typeof raw.skill === 'string' && SKILL_KEYS.includes(raw.skill)
    ? raw.skill
    : null;
  const emote = (Number.isInteger(raw.emote) && raw.emote >= 1 && raw.emote <= 6)
    ? raw.emote
    : null;
  return {
    seq: Number.isFinite(raw.seq) ? (raw.seq | 0) : 0,
    moveX: num(raw.moveX),
    moveY: num(raw.moveY),
    aimAngle: num(raw.aimAngle),
    attack: !!raw.attack,
    skill,
    emote,
  };
}
```

- [ ] **Step 3: 為 Items applyInput 末段呼叫 helper**

頂端 import 加：

```js
import { applyEmoteInput } from '../../emotes.js';
```

跟 BR 一樣，`applyInput`（line 119）開頭有 `if (!p || !p.alive || p.paused) return state;`。改成：

```js
export function applyInput(state, playerId, input, now, rng = Math.random) {
  const p = state.players[playerId];
  if (!p) return state;
  if (!p.alive || p.paused) {
    applyEmoteInput(p, input, state, now);
    return state;
  }

  // 凍結中不能動 / 不能射 / 不能施技 ... [既有邏輯不動]
  // ... (frozen / move / aim / attack / skill 全保留) ...

  applyEmoteInput(p, input, state, now);

  return state;
}
```

末段在 `return state;` 前加 `applyEmoteInput(p, input, state, now);`。

- [ ] **Step 4: 寫整合測試**

`packages/shared/test/games/items/simulation.test.js` 末尾加：

```js
test('items/applyInput：合法 emote → state.events 有 emote 事件', () => {
  const s = createInitialState(players2, {}, 0);
  applyInput(s, 'p1', emptyInput({ emote: 4 }), 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 1);
  assert.equal(emoteEvents[0].slot, 4);
});

test('items/applyInput：玩家凍結中仍能 emote', () => {
  const s = createInitialState(players2, {}, 0);
  s.players.p1.frozenUntil = 9999999;
  applyInput(s, 'p1', emptyInput({ emote: 3 }), 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 1);
});

test('items/applyInput：paused 玩家不能 emote', () => {
  const s = createInitialState(players2, {}, 0);
  s.players.p1.paused = true;
  applyInput(s, 'p1', emptyInput({ emote: 1 }), 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 0);
});
```

注意：先確認該 test 檔內已有 `players2` 與 `emptyInput` 工廠；如沒有，仿 br 那個檔自己加：

```js
const players2 = [
  { id: 'p1', characterId: 'munchkin' },
  { id: 'p2', characterId: 'husky' },
];
function emptyInput(over = {}) {
  return { seq: 1, moveX: 0, moveY: 0, aimAngle: 0, attack: false, skill: null, ...over };
}
```

- [ ] **Step 5: 跑 Items 測試確認通過**

```bash
node --test packages/shared/test/games/items/simulation.test.js
```

預期全 PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/games/items/simulation.js packages/shared/test/games/items/simulation.test.js
git commit -m "feat(items): applyInput 接 emote helper、createInitialState 加 emoteCdUntil"
```

---

## Task 5: Territory sim 接入 emote

**Files:**
- Modify: `packages/shared/src/games/territory/simulation.js`

- [ ] **Step 1: 為 Territory createInitialState 加 emoteCdUntil 欄位**

找 `createInitialState` 的 `state.players[pid] = {...}`（line 81-89），改成：

```js
      state.players[pid] = {
        id: pid,
        characterId: spec?.characterId,
        teamId: team.id,
        x: cx + 0.5, y: cy + 0.5,
        moveX: 0, moveY: 0,
        aimAngle: 0, facing: 0,
        alive: true, paused: false,
        emoteCdUntil: 0,
      };
```

- [ ] **Step 2: 為 Territory sanitizeInput 加 emote 白名單**

`sanitizeInput`（line 114-123）改成：

```js
export function sanitizeInput(raw) {
  if (!raw || typeof raw !== 'object') return null;
  const num = (v) => (Number.isFinite(v) ? v : 0);
  const emote = (Number.isInteger(raw.emote) && raw.emote >= 1 && raw.emote <= 6)
    ? raw.emote
    : null;
  return {
    seq: Number.isFinite(raw.seq) ? (raw.seq | 0) : 0,
    moveX: num(raw.moveX),
    moveY: num(raw.moveY),
    aimAngle: num(raw.aimAngle),
    emote,
  };
}
```

- [ ] **Step 3: 為 Territory applyInput 末段呼叫 helper**

頂端 import 加：

```js
import { applyEmoteInput } from '../../emotes.js';
```

`applyInput`（line 128）改成：

```js
export function applyInput(state, playerId, input, now, _rng) {
  const p = state.players[playerId];
  if (!p) return state;
  if (!p.alive || p.paused) {
    applyEmoteInput(p, input, state, now);
    return state;
  }
  const mx = input.moveX ?? 0, my = input.moveY ?? 0;
  const len = Math.hypot(mx, my);
  if (len > 0) { p.moveX = mx / len; p.moveY = my / len; }
  else { p.moveX = 0; p.moveY = 0; }
  if (typeof input.aimAngle === 'number') {
    p.aimAngle = input.aimAngle;
    p.facing = input.aimAngle;
  }
  applyEmoteInput(p, input, state, now);
  return state;
}
```

- [ ] **Step 4: 寫整合測試**

`packages/shared/test/games/territory/simulation.test.js` 末尾加（如該檔已有 `players2` / 工廠就 reuse；沒有就自加）：

```js
test('territory/applyInput：合法 emote → state.events 有 emote 事件', () => {
  // 用 4 人 → 2v2，避免 1v1 trivial
  const players = [
    { id: 'p1', characterId: 'munchkin' },
    { id: 'p2', characterId: 'husky' },
    { id: 'p3', characterId: 'tabby' },
    { id: 'p4', characterId: 'beagle' },
  ];
  const s = createInitialState(players, {}, 0);
  applyInput(s, 'p1', { seq: 1, moveX: 0, moveY: 0, aimAngle: 0, emote: 5 }, 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 1);
  assert.equal(emoteEvents[0].slot, 5);
});

test('territory/applyInput：paused 不能 emote', () => {
  const players = [
    { id: 'p1', characterId: 'munchkin' },
    { id: 'p2', characterId: 'husky' },
  ];
  const s = createInitialState(players, {}, 0);
  s.players.p1.paused = true;
  applyInput(s, 'p1', { seq: 1, moveX: 0, moveY: 0, aimAngle: 0, emote: 1 }, 1000);
  const emoteEvents = s.events.filter(e => e.kind === 'emote' && e.playerId === 'p1');
  assert.equal(emoteEvents.length, 0);
});
```

- [ ] **Step 5: 跑 Territory 測試確認通過**

```bash
node --test packages/shared/test/games/territory/simulation.test.js
```

預期全 PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/games/territory/simulation.js packages/shared/test/games/territory/simulation.test.js
git commit -m "feat(territory): applyInput 接 emote helper、createInitialState 加 emoteCdUntil"
```

---

## Task 6: inputSchema.test.js — 三款白名單測試

**Files:**
- Modify: `packages/shared/test/games/inputSchema.test.js`

- [ ] **Step 1: 為三款 sanitizeInput 加 emote 測試**

在檔尾追加（reuse 檔案頂端既有的 `sanitizeBR / sanitizeItems / sanitizeTerritory` import，不要重 import）：

```js
/* ---- emote 白名單（三款共用規則）---- */

test('br/sanitizeInput：合法 emote 1-6 保留', () => {
  for (let s = 1; s <= 6; s++) {
    assert.equal(sanitizeBR({ emote: s }).emote, s);
  }
});

test('br/sanitizeInput：emote 越界 → null', () => {
  assert.equal(sanitizeBR({ emote: 0 }).emote, null);
  assert.equal(sanitizeBR({ emote: 7 }).emote, null);
  assert.equal(sanitizeBR({ emote: -1 }).emote, null);
});

test('br/sanitizeInput：emote 非整數 → null', () => {
  assert.equal(sanitizeBR({ emote: 1.5 }).emote, null);
  assert.equal(sanitizeBR({ emote: 'evil' }).emote, null);
  assert.equal(sanitizeBR({ emote: {} }).emote, null);
  assert.equal(sanitizeBR({ emote: true }).emote, null);
});

test('br/sanitizeInput：emote 不存在 → null', () => {
  assert.equal(sanitizeBR({}).emote, null);
});

test('items/sanitizeInput：合法 emote 1-6 保留', () => {
  assert.equal(sanitizeItems({ emote: 3 }).emote, 3);
});

test('items/sanitizeInput：emote 7 → null', () => {
  assert.equal(sanitizeItems({ emote: 7 }).emote, null);
});

test('territory/sanitizeInput：合法 emote 保留', () => {
  assert.equal(sanitizeTerritory({ emote: 6 }).emote, 6);
});

test('territory/sanitizeInput：emote 非法 → null', () => {
  assert.equal(sanitizeTerritory({ emote: 'evil' }).emote, null);
  assert.equal(sanitizeTerritory({ emote: 0 }).emote, null);
});
```

- [ ] **Step 2: 跑 inputSchema 測試**

```bash
node --test packages/shared/test/games/inputSchema.test.js
```

預期全 PASS。

- [ ] **Step 3: 跑 shared 全測試（確認沒有副作用）**

```bash
npm test --workspace @office-colosseum/shared
```

預期全 PASS。

- [ ] **Step 4: Commit**

```bash
git add packages/shared/test/games/inputSchema.test.js
git commit -m "test(shared): 三款 sanitizeInput 加 emote 白名單測試"
```

---

# Phase B：Client UI primitives

## Task 7: index.html 加 emoteBubbleFloat keyframe

**Files:**
- Modify: `packages/client/index.html`

`floatUp` keyframe 用了 `translateX(-50%)`，那是給 HTML absolute 定位的。SVG 的 `<g>` 已經被 attribute `transform` 定位，再用 CSS transform 會打架。需要新增一個 SVG 友善的 keyframe，動畫 `transform-box: fill-box` + `translate` 不影響原 attribute。

**注意：SVG 元素的 CSS `transform` 在 Chrome/Firefox 行為現代化（支持 transform-box），可動 `<g>` 內部的 inner `<g>`。EmoteBubble 會用 outer `<g transform="translate(x,y)">` 定位，inner `<g>` 跑 CSS animation。**

- [ ] **Step 1: 在 index.html 既有 keyframes 區塊內加新 keyframe**

找 `@keyframes floatUp { ... }` 那行（line 76 附近），下方加：

```css
@keyframes emoteBubbleFloat {
  0%   { opacity: 0; transform: translateY(0.2px); }
  10%  { opacity: 1; transform: translateY(0); }
  85%  { opacity: 1; transform: translateY(-0.7px); }
  100% { opacity: 0; transform: translateY(-1px); }
}
```

單位是 viewBox 的 user units。translateY 用 0.2~1 的小值，因為三款 viewBox 的高度都在 9–14 之間，這個 offset 視覺上是「往上飄一格」。

- [ ] **Step 2: Commit**

```bash
git add packages/client/index.html
git commit -m "feat(client): index.html 加 emoteBubbleFloat keyframe（SVG 用）"
```

---

## Task 8: useEmoteInput hook

**Files:**
- Create: `packages/client/src/screens/battle/useEmoteInput.js`

- [ ] **Step 1: 寫 hook 內容**

新建 `packages/client/src/screens/battle/useEmoteInput.js`：

```js
// 戰鬥畫面共用：監聽 hold T + 數字 1-6 觸發 emote。
// - hold T 期間 emoteOpen = true，畫面下方 EmoteBar 顯示。
// - hold 期間按 1-6 → 寫入 pendingRef，由父元件每 tick consume()。
// - preventDefault + stopPropagation 在 hold T 期間攔截 1-6，避免 Items 的技能誤觸。
// - input/textarea 有 focus 時整組 disabled（chat composer 不誤觸）。
// - window blur 時 emoteOpen 自動 reset。

import { useCallback, useEffect, useRef, useState } from 'react';

const SLOT_RE = /^Digit[1-6]$/;

export function useEmoteInput() {
  const [emoteOpen, setEmoteOpen] = useState(false);
  const emoteOpenRef = useRef(false);
  const pendingRef = useRef(null);

  // 同步 ref 與 state（事件 handler 用 ref 讀）
  useEffect(() => { emoteOpenRef.current = emoteOpen; }, [emoteOpen]);

  useEffect(() => {
    function isFormFocus(target) {
      if (!target) return false;
      const tag = target.tagName;
      return tag === 'INPUT' || tag === 'TEXTAREA' || target.isContentEditable;
    }

    const onKeyDown = (e) => {
      if (isFormFocus(e.target)) return;
      if (e.code === 'KeyT') {
        if (!e.repeat) setEmoteOpen(true);
        return;
      }
      if (emoteOpenRef.current && SLOT_RE.test(e.code)) {
        const slot = parseInt(e.code.slice(-1), 10);
        if (slot >= 1 && slot <= 6) pendingRef.current = slot;
        e.preventDefault();
        e.stopPropagation();
      }
    };
    const onKeyUp = (e) => {
      if (e.code === 'KeyT') setEmoteOpen(false);
    };
    const onBlur = () => setEmoteOpen(false);

    window.addEventListener('keydown', onKeyDown);
    window.addEventListener('keyup', onKeyUp);
    window.addEventListener('blur', onBlur);
    return () => {
      window.removeEventListener('keydown', onKeyDown);
      window.removeEventListener('keyup', onKeyUp);
      window.removeEventListener('blur', onBlur);
    };
  }, []);

  const consume = useCallback(() => {
    const v = pendingRef.current;
    pendingRef.current = null;
    return v;
  }, []);

  return { emoteOpen, consume };
}
```

- [ ] **Step 2: 確認檔案 syntax 正確（vite build 一次）**

```bash
npm run build --workspace @office-colosseum/client
```

預期：build 成功（dist/ 出檔）。若 fail，檢查 import path / JSX 語法。

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/screens/battle/useEmoteInput.js
git commit -m "feat(client): useEmoteInput hook — hold T + 1-6 偵測"
```

---

## Task 9: useEmoteFeed hook

**Files:**
- Create: `packages/client/src/screens/battle/useEmoteFeed.js`

- [ ] **Step 1: 寫 hook 內容**

新建 `packages/client/src/screens/battle/useEmoteFeed.js`：

```js
// 訂閱 SNAPSHOT 累積目前活著的 emote。
// 內部 state 是 { [playerId]: { slot, startedAt, expiresAt } }。
// 每 frame（rAF）過期清理；event.atMs 來自 server，所以 expiresAt 也是 server 時鐘。
// 為避免 client / server 時鐘漂移過久，過期判定用「Date.now() > expiresAt + tolerance」其實沒必要——
// emote 只活 2.5s，漂移幾百 ms 不影響玩家觀感，直接 client now 比即可。

import { useEffect, useRef, useState } from 'react';
import { MSG } from '@office-colosseum/shared';
import { getSocket } from '../../net/socket.js';

const EMOTE_DURATION_MS = 2500;

export function useEmoteFeed() {
  const [active, setActive] = useState({});
  const activeRef = useRef({});
  const rafRef = useRef(0);

  // 同步 ref 與 state，handler 用 ref 寫
  useEffect(() => { activeRef.current = active; }, [active]);

  useEffect(() => {
    const socket = getSocket();
    let dirty = false;

    const onSnapshot = (snap) => {
      const events = snap?.events;
      if (!Array.isArray(events) || events.length === 0) return;
      let mutated = null;
      for (const e of events) {
        if (e.kind !== 'emote') continue;
        if (mutated === null) mutated = { ...activeRef.current };
        const startedAt = Number.isFinite(e.atMs) ? e.atMs : Date.now();
        mutated[e.playerId] = {
          slot: e.slot,
          startedAt,
          expiresAt: startedAt + EMOTE_DURATION_MS,
        };
      }
      if (mutated !== null) {
        activeRef.current = mutated;
        dirty = true;
      }
    };

    function tick() {
      const now = Date.now();
      const cur = activeRef.current;
      let removed = null;
      for (const [pid, e] of Object.entries(cur)) {
        if (now > e.expiresAt) {
          if (removed === null) removed = { ...cur };
          delete removed[pid];
        }
      }
      if (removed !== null) {
        activeRef.current = removed;
        dirty = true;
      }
      if (dirty) {
        dirty = false;
        setActive(activeRef.current);
      }
      rafRef.current = requestAnimationFrame(tick);
    }

    socket.on(MSG.SNAPSHOT, onSnapshot);
    rafRef.current = requestAnimationFrame(tick);
    return () => {
      socket.off(MSG.SNAPSHOT, onSnapshot);
      cancelAnimationFrame(rafRef.current);
    };
  }, []);

  return active;
}
```

- [ ] **Step 2: build 一次確認 syntax**

```bash
npm run build --workspace @office-colosseum/client
```

預期 OK。

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/screens/battle/useEmoteFeed.js
git commit -m "feat(client): useEmoteFeed hook — 訂閱 SNAPSHOT 累積活著的 emote"
```

---

## Task 10: EmoteBar component

**Files:**
- Create: `packages/client/src/components/EmoteBar.jsx`

- [ ] **Step 1: 寫元件**

新建 `packages/client/src/components/EmoteBar.jsx`：

```jsx
// hold T 期間出現在畫面下方的 6 格 emote bar。
// 試算表偽裝風：白底 + 細黑線 + 等寬字體 + 無圓角。
// cooldown 中：整條變灰並顯示倒數秒。

import { EMOTES } from '@office-colosseum/shared';

export default function EmoteBar({ open, cooldownUntil = 0 }) {
  if (!open) return null;
  const now = Date.now();
  const inCooldown = cooldownUntil > now;
  const remainSec = inCooldown ? Math.max(0, Math.ceil((cooldownUntil - now) / 1000)) : 0;

  return (
    <div
      style={{
        position: 'fixed',
        bottom: 16,
        left: '50%',
        transform: 'translateX(-50%)',
        zIndex: 6000,
        display: 'flex',
        gap: 0,
        background: 'var(--bg-paper)',
        border: '1px solid var(--ink)',
        boxShadow: '0 2px 6px rgba(0,0,0,0.18)',
        fontFamily: 'var(--font-mono)',
        fontSize: 11,
        opacity: inCooldown ? 0.55 : 1,
        filter: inCooldown ? 'grayscale(0.8)' : 'none',
        pointerEvents: 'none',  // 純展示，不擋滑鼠
        userSelect: 'none',
      }}
    >
      {EMOTES.map((e) => (
        <div
          key={e.slot}
          style={{
            padding: '4px 10px',
            borderRight: '1px solid var(--line-soft)',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            minWidth: 56,
          }}
        >
          <span style={{ color: 'var(--ink-muted)', fontSize: 9 }}>[{e.key}]</span>
          <span style={{ color: 'var(--ink)', fontSize: 13, lineHeight: 1.1 }}>{e.kaomoji}</span>
          <span style={{ color: 'var(--ink-muted)', fontSize: 9 }}>{e.label}</span>
        </div>
      ))}
      {inCooldown && (
        <div style={{
          padding: '4px 10px',
          background: 'var(--accent-danger)',
          color: 'var(--bg-paper)',
          fontSize: 11,
          alignSelf: 'stretch',
          display: 'flex',
          alignItems: 'center',
        }}>
          CD {remainSec}s
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: build 確認**

```bash
npm run build --workspace @office-colosseum/client
```

預期 OK。

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/components/EmoteBar.jsx
git commit -m "feat(client): EmoteBar — hold T 期間下方 6 格 bar UI"
```

---

## Task 11: EmoteBubble component

**Files:**
- Create: `packages/client/src/components/EmoteBubble.jsx`

- [ ] **Step 1: 寫元件**

新建 `packages/client/src/components/EmoteBubble.jsx`：

```jsx
// SVG 頭頂氣泡，渲染在 Arena viewBox 內、世界座標跟著玩家走。
// 外層 <g> 用 attribute transform 定位，內層 <g> 跑 CSS animation
// （outer transform 不能被 inner CSS animation 覆寫）。
// 動畫 2.5s 飄上 + fade out，animation-fill-mode: forwards 讓結束時保持透明。
//
// 視覺實作選擇：用 text + 白色 stroke + paint-order:stroke 取代 rect 背景。
// 等效於「白底黑字」但寬度自適應顏文字內容（最寬翻桌 11 字）；省去手動量寬度。
// 仍保留三角 tail 維持氣泡視覺。

import { EMOTES } from '@office-colosseum/shared';

// Note: startedAtMs 沒進 props — 動畫由 mount 時的 keyframe `forwards` 控制。
// Arena 那層用 `key={"emote-" + pid + "-" + e.startedAt}` 強制 remount，
// 達成「同 player 後發 emote 重置動畫」的效果。

export default function EmoteBubble({ x, y, slot }) {
  const emote = EMOTES.find(e => e.slot === slot);
  if (!emote) return null;

  return (
    <g transform={`translate(${x}, ${y - 0.85})`}>
      <g style={{
        animation: 'emoteBubbleFloat 2.5s ease-out forwards',
        transformOrigin: 'center',
      }}>
        {/* 白色描邊 + 黑字，視覺上像是白底黑邊 */}
        <text
          textAnchor="middle"
          dominantBaseline="middle"
          fontFamily="var(--font-mono, monospace)"
          fontSize="0.42"
          fill="var(--ink, #1a1a1a)"
          stroke="var(--bg-paper, #ffffff)"
          strokeWidth="0.12"
          paintOrder="stroke"
          style={{ userSelect: 'none' }}
        >
          {emote.kaomoji}
        </text>
        {/* 朝下三角 tail */}
        <path
          d="M -0.08 0.2 L 0.08 0.2 L 0 0.32 Z"
          fill="var(--bg-paper, #ffffff)"
          stroke="var(--ink, #1a1a1a)"
          strokeWidth="0.018"
        />
      </g>
    </g>
  );
}
```

- [ ] **Step 2: build 確認**

```bash
npm run build --workspace @office-colosseum/client
```

預期 OK。

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/components/EmoteBubble.jsx
git commit -m "feat(client): EmoteBubble — SVG 頭頂顏文字氣泡（floatUp 動畫）"
```

---

# Phase C：三款戰鬥整合

## Task 12: useInputItems 接 emoteOpen 並抑制 1-5

**Files:**
- Modify: `packages/client/src/screens/battle/items/useInputItems.js`

- [ ] **Step 1: 改 hook signature 並擋 1-5**

把 `useInputItems(arenaRef, selfPosRef)` 改成 `useInputItems(arenaRef, selfPosRef, options = {})`，options 含 `emoteOpenRef`（傳 ref 是因為 onKeyDown 是 useEffect 內 long-lived，要讀最新值）。

完整改寫 hook 開頭：

```js
export function useInputItems(arenaRef, selfPosRef, options = {}) {
  const { emoteOpenRef } = options;  // 可為 undefined（向後相容）
  const keys = useRef(new Set());
  const leftDown = useRef(false);
  const pendingSkill = useRef(null);
  const mouseWorld = useRef({ x: ARENA_COLS / 2, y: ARENA_ROWS / 2 });
  const seq = useRef(0);

  useEffect(() => {
    const onKeyDown = (e) => {
      const k = e.key.toLowerCase();
      keys.current.add(k);
      // hold T 中 → 1-5 不寫 pendingSkill（讓 useEmoteInput 收）
      const tHeld = !!emoteOpenRef?.current;
      if (!tHeld && ['1', '2', '3', '4', '5'].includes(e.key)) {
        const idx = parseInt(e.key, 10) - 1;
        pendingSkill.current = SKILL_KEYS[idx] ?? null;
      }
    };
    // ... 其餘不動
```

注意：onKeyDown 接收 `keys.current.add(k)` 仍然把 `1`–`5` 加到 keys set —— 這 OK，因為這些 key 沒在 readInput 裡被讀取（readInput 只讀 wasd / arrow）。Items 沒用 1-5 走移動邏輯。

- [ ] **Step 2: build 確認**

```bash
npm run build --workspace @office-colosseum/client
```

預期 OK。

- [ ] **Step 3: Commit**

```bash
git add packages/client/src/screens/battle/items/useInputItems.js
git commit -m "feat(items): useInputItems 接 emoteOpenRef、hold T 期間擋 1-5 技能"
```

---

## Task 13: BR / Territory useInput passthrough emoteOpen

**Files:**
- Modify: `packages/client/src/screens/battle/br/useInputBR.js`
- Modify: `packages/client/src/screens/battle/territory/useInputTerritory.js`

兩個 hook 沒用 1-6 鍵，只需把 signature 統一以利父層整合。

- [ ] **Step 1: BR useInputBR signature 加 options**

把 `useInputBR(arenaRef, selfPosRef)` 改成 `useInputBR(arenaRef, selfPosRef, _options = {})`：

```js
export function useInputBR(arenaRef, selfPosRef, _options = {}) {
  // _options 預留未來使用；BR 不用 1-6 故無需處理 emoteOpen
  // ... 既有邏輯完全不動 ...
```

- [ ] **Step 2: Territory useInputTerritory signature 加 options**

```js
export function useInputTerritory(_options = {}) {
  // ... 既有邏輯不動 ...
```

- [ ] **Step 3: build 確認**

```bash
npm run build --workspace @office-colosseum/client
```

預期 OK。

- [ ] **Step 4: Commit**

```bash
git add packages/client/src/screens/battle/br/useInputBR.js packages/client/src/screens/battle/territory/useInputTerritory.js
git commit -m "refactor(client): BR/Territory useInput 加 _options 預留 arg"
```

---

## Task 14: BR 整合（BattleRoyale.jsx + ArenaBR.jsx）

**Files:**
- Modify: `packages/client/src/screens/battle/br/BattleRoyale.jsx`
- Modify: `packages/client/src/screens/battle/br/ArenaBR.jsx`

- [ ] **Step 1: BattleRoyale.jsx 整合 useEmoteInput / useEmoteFeed / EmoteBar**

頂端 import 區加：

```jsx
import { EMOTE_CD_MS } from '@office-colosseum/shared';
import { useEmoteInput } from '../useEmoteInput.js';
import { useEmoteFeed } from '../useEmoteFeed.js';
import EmoteBar from '../../../components/EmoteBar.jsx';
```

在 `BattleRoyale` 元件內、`const readInput = useInputBR(...)` 之前 / 之後加：

```jsx
  const { emoteOpen, consume: consumeEmote } = useEmoteInput();
  const activeEmotes = useEmoteFeed();
  const [selfCooldownUntil, setSelfCooldownUntil] = useState(0);
```

把送 INPUT 的 `setInterval` 內改成：

```jsx
    const id = setInterval(() => {
      if (phase !== 'playing') return;
      try {
        const baseInput = readInput();
        const emote = consumeEmote();
        if (emote != null && Date.now() >= selfCooldownUntil) {
          setSelfCooldownUntil(Date.now() + EMOTE_CD_MS);
        }
        socket.emit(MSG.INPUT, { ...baseInput, emote });
      } catch (e) { /* ignore */ }
    }, TICK_MS);
```

注意 deps 陣列要加 `selfCooldownUntil` 和 `consumeEmote`：

```jsx
  }, [phase, readInput, socket, readOnly, consumeEmote, selfCooldownUntil]);
```

把 `<ArenaBR ...>` 改成多帶 `activeEmotes` prop：

```jsx
          <ArenaBR
            ref={arenaRef}
            map={initialState?.state?.map}
            players={players}
            bullets={bullets}
            poison={poison}
            selfId={selfId}
            hurtIds={hurtIds}
            activeEmotes={activeEmotes}
          />
```

在 SheetWindow 內最後（`</BattleHudBR>` 之後）加：

```jsx
      <EmoteBar open={emoteOpen} cooldownUntil={selfCooldownUntil} />
```

- [ ] **Step 2: ArenaBR.jsx 渲染 EmoteBubble 層**

頂端 import 加：

```jsx
import EmoteBubble from '../../../components/EmoteBubble.jsx';
```

`ArenaBR` 元件 signature 加 `activeEmotes`：

```jsx
const ArenaBR = memo(forwardRef(function ArenaBR(
  { map, players, bullets, poison, selfId, hurtIds, activeEmotes = {} },
  ref,
) {
```

在 `playerEls` 那段之後、`bulletEls` 之前（或 SVG 結尾 `</svg>` 前）加 emote 層：

```jsx
  const emoteEls = Object.entries(activeEmotes).map(([pid, e]) => {
    const p = renderPlayers?.[pid];
    if (!p) return null;
    return (
      <EmoteBubble
        key={`emote-${pid}-${e.startedAt}`}
        x={p.x}
        y={p.y}
        slot={e.slot}
      />
    );
  });
```

在 SVG 內 players 之後加：

```jsx
      {/* emote bubbles（最上層） */}
      {emoteEls}
    </svg>
```

- [ ] **Step 3: 跑 dev server 手動驗證 BR**

開兩個 terminal：

```bash
# Terminal 1
npm run dev:server
```

```bash
# Terminal 2
npm run dev:client
```

開兩個瀏覽器 tab：
- Tab A 用 admin 登入、進大廳開房選 BR、加 1 個 bot、ready、START
- 觀察戰鬥畫面，按 hold T → 下方 EmoteBar 出現
- 按 6（翻桌）→ 自己頭頂應顯示 `(╯°□°)╯︵ ┻━┻` 約 2.5s 飄上消失
- 放開 T → bar 消失
- 立刻再按 T+1 → bar 變灰、不發 emote（還在 cooldown）
- 等 3 秒後再 T+1 → 再發 `(´∀｀)`

若有任何不符，回去檢查：
- console error？
- WebSocket frame：socket.emit 有沒有帶 `emote` 欄位？（DevTools Network → WS）
- server log：sanitizeInput 有沒有正確收到？

- [ ] **Step 4: Commit**

```bash
git add packages/client/src/screens/battle/br/BattleRoyale.jsx packages/client/src/screens/battle/br/ArenaBR.jsx
git commit -m "feat(br): BattleRoyale 整合 emote — hold T + 1-6、頭頂氣泡"
```

---

## Task 15: Items 整合（ItemsBattle.jsx + ArenaItems.jsx）

**Files:**
- Modify: `packages/client/src/screens/battle/items/ItemsBattle.jsx`
- Modify: `packages/client/src/screens/battle/items/ArenaItems.jsx`

- [ ] **Step 1: 讀 ItemsBattle.jsx 確認 useInputItems 呼叫位置**

```bash
grep -n 'useInputItems' packages/client/src/screens/battle/items/ItemsBattle.jsx
```

- [ ] **Step 2: ItemsBattle.jsx 整合**

頂端 import 加（仿 BR）：

```jsx
import { useRef } from 'react';  // 如果還沒
import { EMOTE_CD_MS } from '@office-colosseum/shared';
import { useEmoteInput } from '../useEmoteInput.js';
import { useEmoteFeed } from '../useEmoteFeed.js';
import EmoteBar from '../../../components/EmoteBar.jsx';
```

組件內加：

```jsx
  const { emoteOpen, consume: consumeEmote } = useEmoteInput();
  const emoteOpenRef = useRef(false);
  // 同步 emoteOpen 到 ref（給 useInputItems 在事件 handler 裡讀）
  useEffect(() => { emoteOpenRef.current = emoteOpen; }, [emoteOpen]);
  const activeEmotes = useEmoteFeed();
  const [selfCooldownUntil, setSelfCooldownUntil] = useState(0);

  const readInput = useInputItems(arenaRef, selfPosRef, { emoteOpenRef });
```

INPUT setInterval 內改：

```jsx
    const id = setInterval(() => {
      if (phase !== 'playing') return;
      try {
        const baseInput = readInput();
        const emote = consumeEmote();
        if (emote != null && Date.now() >= selfCooldownUntil) {
          setSelfCooldownUntil(Date.now() + EMOTE_CD_MS);
        }
        socket.emit(MSG.INPUT, { ...baseInput, emote });
      } catch (e) { /* ignore */ }
    }, TICK_MS);
```

`<ArenaItems ...>` 加 `activeEmotes={activeEmotes}` prop。

JSX 結尾（SheetWindow 收前 / 後）加：

```jsx
      <EmoteBar open={emoteOpen} cooldownUntil={selfCooldownUntil} />
```

- [ ] **Step 3: ArenaItems.jsx 渲染 EmoteBubble**

```bash
grep -n 'function ArenaItems' packages/client/src/screens/battle/items/ArenaItems.jsx
```

仿 ArenaBR：
- import EmoteBubble
- signature 加 `activeEmotes = {}`
- SVG 內 players 之後加 emoteEls 渲染

- [ ] **Step 4: build + dev 手動驗證 Items**

```bash
npm run build --workspace @office-colosseum/client
```

開 dev 環境，建一個 Items 房間 + 1 bot：
- 不按 T，按 1/2/3/4/5 → 應該觸發既有的技能（freeze / undo / merge / readonly / validate），驗證沒被 emote 系統誤吃
- hold T 期間按 1-5 → **不應觸發技能**，應該發 emote
- 觀察 server log：`emote` 事件 push 進 events
- 觀察觀戰者：另開一個 tab 登第 3 個帳號，從房間列表觀戰該局 → 應看到玩家頭頂有 emote 氣泡（前提是 SpectatorBattle 也吃 SNAPSHOT events，下個 task 補）

- [ ] **Step 5: Commit**

```bash
git add packages/client/src/screens/battle/items/ItemsBattle.jsx packages/client/src/screens/battle/items/ArenaItems.jsx
git commit -m "feat(items): ItemsBattle 整合 emote、hold T 抑制 1-5 技能"
```

---

## Task 16: Territory 整合（TerritoryBattle.jsx + ArenaTerritory.jsx）

**Files:**
- Modify: `packages/client/src/screens/battle/territory/TerritoryBattle.jsx`
- Modify: `packages/client/src/screens/battle/territory/ArenaTerritory.jsx`

仿照 Task 14 / 15。Territory 沒滑鼠輸入，但其餘整合一致。

- [ ] **Step 1: TerritoryBattle.jsx 整合**

頂端 import 加 `EMOTE_CD_MS`、`useEmoteInput`、`useEmoteFeed`、`EmoteBar`。

組件內加：

```jsx
  const { emoteOpen, consume: consumeEmote } = useEmoteInput();
  const activeEmotes = useEmoteFeed();
  const [selfCooldownUntil, setSelfCooldownUntil] = useState(0);
```

INPUT setInterval 改：

```jsx
        const baseInput = readInput();
        const emote = consumeEmote();
        if (emote != null && Date.now() >= selfCooldownUntil) {
          setSelfCooldownUntil(Date.now() + EMOTE_CD_MS);
        }
        socket.emit(MSG.INPUT, { ...baseInput, emote });
```

`<ArenaTerritory ...>` 加 `activeEmotes={activeEmotes}` prop。

JSX 收前加 `<EmoteBar open={emoteOpen} cooldownUntil={selfCooldownUntil} />`。

- [ ] **Step 2: ArenaTerritory.jsx 渲染 EmoteBubble**

仿 ArenaBR / ArenaItems。

- [ ] **Step 3: build + dev 手動驗證 Territory**

開 4 人房（2v2），自己 + 3 個 bot：
- 按 hold T → bar 出現
- 按 1-6 → 自己頭頂顯示對應顏文字
- 因 territory 沒 1-5 衝突，行為跟 BR 一樣
- 觀察 paint 事件正常運作（emote 不影響移動 / 塗色）

- [ ] **Step 4: Commit**

```bash
git add packages/client/src/screens/battle/territory/TerritoryBattle.jsx packages/client/src/screens/battle/territory/ArenaTerritory.jsx
git commit -m "feat(territory): TerritoryBattle 整合 emote"
```

---

# Phase D：Smoke + 驗收

## Task 17: 觀戰路徑驗證

**Files:**
- 無需修改（SpectatorBattle 透過共用 BattleXxx 元件自動 work）

- [ ] **Step 1: 手動驗證觀戰看得到 emote**

開三個 tab：
- A 進房開戰
- B 進房當第 2 個玩家
- 第 3 個 tab（C，admin 或第三玩家）從大廳列表點 A 那房「觀戰」按鈕

A 在戰鬥中 hold T+6 翻桌：
- B 應看到 A 頭頂顯示 `(╯°□°)╯︵ ┻━┻`
- C（觀戰）應**也**看到 A 頭頂的 emote

如果觀戰看不到 emote：
- 檢查 `useEmoteFeed()` 在 SpectatorBattle path 是否被呼叫（透過 BattleXxx 元件繼承，照理應該 work）
- 開 DevTools 檢查 SPECTATE_INIT 後的 SNAPSHOT events 是否有 `kind:'emote'`
- 若 server 沒廣播給觀戰者，看 `match.js` 的 spectator broadcast 有沒有把 events slice 帶進去

- [ ] **Step 2: 處理結果**

- 若觀戰能正確看到 emote → 本 task 無 code 變更，不 commit、進下一 task
- 若觀戰看不到，最常見原因是 `match.js` 對 spectator 廣播 SNAPSHOT 時沒帶 events slice。檢查 `packages/server/src/match.js` 的 spectator 廣播路徑（搜 `SPECTATE_INIT` 或 `to(spectatorRoom)` 相關呼叫），確認 events 有透過 `state.events.slice(eventsStartIdx)` 帶出去
- 若 `useEmoteFeed` 在 SpectatorBattle 路徑中沒被 mount，檢查 BattleXxx 元件是否在 `readOnly === true` 時 short-circuit 掉 `useEmoteFeed()` — 不應該；本 plan 設計上 readOnly 也要訂閱 SNAPSHOT events 才能畫 emote bubble

---

## Task 18: 擴充 smoke test

**Files:**
- Modify: `packages/server/test/smoke.js`

- [ ] **Step 1: smoke.js 加 emote 檢查**

讀 `packages/server/test/smoke.js` 找到 MATCH_START handler（line ~79-87）。

把 `a.on(MSG.MATCH_START, ...)` 區塊內的延遲驗收改成：

```js
  let lobbyUpdates = 0, matchStarted = false, emoteSeen = false;

  // ... [其餘不動] ...

  a.on(MSG.MATCH_START, () => {
    matchStarted = true;
    // match start 後過 100ms 送 emote，再 200ms 收 SNAPSHOT 驗證
    setTimeout(() => {
      a.emit(MSG.INPUT, {
        seq: 1, moveX: 0, moveY: 0, aimAngle: 0,
        attack: false, shield: false, dash: false, emote: 6,
      });
    }, 100);
  });

  a.on(MSG.SNAPSHOT, (snap) => {
    if (snap?.events?.some(e => e.kind === 'emote' && e.slot === 6)) {
      emoteSeen = true;
    }
  });

  // 把原本的延後驗收改判斷 emoteSeen
  setTimeout(() => {
    if (lobbyUpdates < 4) return done('expected lobby updates, got ' + lobbyUpdates);
    if (!matchStarted) return done('no match_start');
    if (!emoteSeen) return done('emote event 未在 SNAPSHOT 內收到');
    pass = true;
    done();
  }, 800);  // 延長一點時間給 emote 場景跑完
```

注意：原本的 lobby ack `setTimeout` 在 MATCH_START 內呼叫 done，新版我們把它移到外層獨立 timer。重新整理整個 smoke 主流程：

讀完 smoke.js 後，重寫 `try { ... }` 區塊使邏輯清楚（保留所有既有功能、加 emote 驗證）。

- [ ] **Step 2: 跑 smoke**

需要 docker compose up postgres redis、env 設好：

```bash
npm run smoke --workspace @office-colosseum/server
```

預期：`SMOKE PASS`。

如果 smoke 因 docker / env 問題跑不起來，這步可標 manual 驗證後跳過 commit emote 驗證部分（或保留只做了「emote 驗證 setup 但 timeout 不阻擋 pass」的安全版本）。

- [ ] **Step 3: Commit**

```bash
git add packages/server/test/smoke.js
git commit -m "test(server): smoke 加 emote SNAPSHOT 驗證場景"
```

---

## Task 19: 全 workspace test 跑一次 + manual QA checklist

- [ ] **Step 1: 全 workspace 測試**

```bash
npm test
```

預期：所有 workspace（shared / server）的測試 PASS。

- [ ] **Step 2: 跑過 manual QA checklist（從 spec §7）**

逐項確認：

- [ ] BR 三人房（自己 + 2 bot）能正常 emote
- [ ] Items 按 T+3 不誤發 readonly trap 技能（衝突隔離）
- [ ] Items 不按 T 直接按 3 仍正常觸發 readonly 技能
- [ ] Territory 4 人房 emote 不影響塗色機制
- [ ] Hold T 時下方 bar 出現、放開消失
- [ ] CD 中 bar 變灰並顯示秒數
- [ ] 觀戰者看得到玩家 emote
- [ ] Bot 不會自己 emote（觀察 5 分鐘對戰，bot 頭頂無顏文字）
- [ ] 老闆鍵期間（ESC）發 emote 失敗
- [ ] 翻桌（slot 6）的 11 字寬氣泡在三款 viewBox 都正確顯示、不被切掉
- [ ] Chat composer focus 中按 T+1 → emote 不發、聊天輸入正常
- [ ] window blur（ALT-tab 出去）後 T 仍按著，回到頁面 bar 不應 stuck

- [ ] **Step 3: 若一切 OK，本 plan 完成；不另 commit（文件層 commit 已分散在每個 task）**

---

# 附錄：Spec ↔ Plan 對應表

| Spec 節 | 對應 Task |
|---------|----------|
| §2 6 個顏文字 slot | Task 1 |
| §3 hold T + 1-6 | Task 8, 12 |
| §3 EmoteBar UI | Task 7, 10 |
| §4 SVG bubble + 2.5s float | Task 11 |
| §5 INPUT.emote 欄位 | Task 3, 4, 5 |
| §5 shared/emotes.js + applyEmoteInput | Task 1, 2 |
| §5 三款 sim 接入 | Task 3, 4, 5 |
| §5 SNAPSHOT events `kind:'emote'` | Task 1（helper push）+ 自然透過 match.js |
| §6 useEmoteInput | Task 8 |
| §6 useEmoteFeed | Task 9 |
| §6 EmoteBar | Task 10 |
| §6 EmoteBubble | Task 11 |
| §6 三款 useInput 微調 | Task 12, 13 |
| §6 三款 BattleXxx 整合 | Task 14, 15, 16 |
| §6 三款 ArenaXxx 動態層 | Task 14, 15, 16 |
| §6 觀戰路徑 | Task 17 |
| §7 邊界處理 | Task 1（helper）+ Task 8（hook） |
| §7 單元測試 | Task 1, 6 |
| §7 三款 sanitize 測試 | Task 6 |
| §7 Smoke | Task 18 |
| §7 Manual QA | Task 19 |
